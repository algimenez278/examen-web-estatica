---
const { num } = Astro.props;
const modalId = `modal${num}`;
---

<style>
  /* Card element */
  .cards__card {
    background: oklch(0.6118 0.0927 126.67);
    border: 1px solid oklch(0.6118 0.0927 126.67);
    border-radius: 8px;
    padding: 15px;
    width: 70px;
    height: 40px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    margin: 0 3px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-weight: 700;
    color: oklch(0.4887 0.0805 242.84);
    user-select: none;
    cursor: pointer;
    position: relative;
  }
  @media (max-width: 400px) {
    .cards {
      justify-content: space-around;
      gap: 8px;
    }
    .cards__card {
      width: 60px;
      height: 38px;
      margin: 0 2px;
      font-size: 14px;
    }
  }
</style>

<article
  class="cards__card"
  tabindex="0"
  role="button"
  aria-haspopup="dialog"
  aria-controls={modalId}
  data-modal={modalId}
>
  Pista {num}
</article>
